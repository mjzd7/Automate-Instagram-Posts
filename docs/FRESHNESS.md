# Freshness & research backlog

> Referenced from `AGENTS.md`. Load when re-verifying volatile facts or dropping in a non-binding research note.

## 16. Freshness (docs that rot)

Volatile claims (model names, tool versions, thresholds) live in `docs/TOOLS.md` §3.5, not as eternal truth elsewhere in this contract.

| Cadence | Action |
|---|---|
| On use | If a tool fails, update `docs/TOOLS.md` §3.5 |
| Weekly (L3 / when user asks) | Re-verify §3.5 rows with sources |
| Never | Silently "update" gates to make life easier |

## Research backlog (non-binding scratchpad)

> Not doctrine. Do not treat items here as required agent behavior. Promote an item into `AGENTS.md` or a `docs/*.md` file only after explicit human acceptance, then delete it from this list.

- (empty — add non-binding research notes here as they come up; promote or delete, don't let them accumulate as silent doctrine)
